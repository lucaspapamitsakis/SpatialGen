# Core medical-image IO / numerics
nibabel>=5.2
numpy>=1.26
scipy>=1.11

# Registration / preprocessing (atlas step). Pick ONE primary; both listed.
antspyx>=0.4.2        # ANTsPy: rigid/affine registration to a template
SimpleITK>=2.3        # fallback / utilities

# Deep learning + segmentation
torch>=2.2
monai>=1.3

# Probabilistic programming (MetaCOG inference engine)
pyro-ppl>=1.9

# Experiment tracking & viz
wandb>=0.17
matplotlib>=3.8
tqdm>=4.66
